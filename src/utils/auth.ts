/* ════════════════════════════════════════
   بذرة كلمة السر لأول تشغيل فقط
   ────────────────────────────────────────
   منذ هذا التحديث، التحقق من كلمة السر لا يتم هنا (كان محلياً في الـ Renderer
   عبر مقارنة نصية مباشرة — غير آمن). المصدر الحقيقي الآن هو bcrypt hash
   مخزَّن في جدول app_settings (مفتاح app_password_hash)، ويُتحقَّق منه فقط
   عبر IPC في electron/auth.ts (dbService.auth.verifyPassword).

   هذا الثابت يُستخدم فقط لتوليد الـ hash الأولي عند أول تشغيل بعد هذا
   التحديث (electron/auth.ts → ensurePasswordSeeded)، لضمان استمرار كلمة
   السر الحالية للمستخدمين الموجودين دون أي انقطاع. تغييرها لاحقاً يتم من
   شاشة الإعدادات، وليس بتعديل هذا الملف.
════════════════════════════════════════ */
export const DEFAULT_PASSWORD = 'garage2026'
